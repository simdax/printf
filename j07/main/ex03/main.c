#include <limits.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char *ft_concat_params(int argc, char **argv);

int main()
{
	int nb;
	char str[];

	char *ft_concat_params(nb, &str);
}
